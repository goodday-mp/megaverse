# DigitalOcean deployment

Set the values shown in .env.example, then update the host with git pull origin main, npm install, and restart the megaverse service. If PM2 is used, run pm2 restart megaverse --update-env. Discord Activities require HTTPS on megaverse.duckdns.org.
